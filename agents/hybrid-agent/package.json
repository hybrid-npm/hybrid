{
	"name": "hybrid-agent",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "pnpm --filter hybrid/agent run dev",
		"build": "pnpm --filter hybrid/agent run build",
		"deploy": "fly deploy"
	},
	"dependencies": {
		"hybrid/agent": "workspace:*"
	},
	"devDependencies": {
		"tsx": "^4.19.3"
	}
}
